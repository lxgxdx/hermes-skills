# tongzhan-info-workflow 进化记录 (2026-05-29)

## 本次更新（6项改进）

基于 47+ 历史会话分析和 2026-05-28/29 多轮对话教训，对 SKILL.md 做了以下改进：

### 1. 排重新增"已写内容"检查
- 新增 `⚠️ 排重第一步：先查"已写内容"` 段落
- 教训："一键投诚"在选题库中标记为"选题建议"，但用户说"内容比较老了"
- 解法：推荐选题前同步检查两层：选题库文件 + 已写内容目录
- 对方向模糊的主动询问用户

### 2. 选题状态标注
- 每个选题输出时标注：✅当日选题库 / ✅新发现热点 / ⚠️仅选题库 / ❌已写过
- 让用户一眼看清重复风险

### 3. GBrain 上下文关联
- 临时选题前先查 GBrain 项目上下文
- 教训：brain 已存大量统战项目但从未关联
- 解法：`gbrain query <方向>` 或 `session_search` 关联历史

### 4. 空洞化标准明确化
- 新增"⚠️ 空洞化预警清单"章节
- 明确"四有"标准：有案例/有数据/有来源/有地方切入角度
- 列出典型空洞特征

### 5. Word 落盘验证
- 生成 docx 后检查 `os.path.getsize()` > 0
- 教训：05/28 选题5、6的 docx 文件大小为 0 字节

### 6. DeepSeek API 验证
- 调用后验证 `content` 不为空
- 若空则提醒加 `"reasoning":"skip"`

## 后续建议优化方向

| 问题 | 状态 | 建议 |
|------|------|------|
| SKILL.md ~1200行/73KB，远超8-15k目标 | ⏳ 进行中 | 拆分到 references/ 子文件 |
| 主题7.0/7.1/7.7重复Searxng问题 | ✅ **2026-05-31 已合并** | 合并为一个"7.0 cron环境网络限制与兜底策略"总节，含网络限制一览、脚本路径、外网兜底优先级、维度交叉法 |
| "临时选题"和"cron推送"流程交错 | ⏳ 待处理 | 应分两个独立小节，不混编 |
| 大量代码示例（python-docx、docx格式模板） | ⏳ 待处理 | 提取到 scripts/ 目录 |
| 排重规则有md和xlsx两种格式 | ⏳ 待处理 | 统一到一种格式 |
